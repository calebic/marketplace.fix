<Controls:MetroWindow x:Class="WpfApp1.BrandBreakdownWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      Title="Brand Breakdown"
                      Width="1080"
                      Height="720"
                      MinWidth="900"
                      MinHeight="560"
                      GlowBrush="#2A7BE4"
                      WindowTransitionsEnabled="False"
                      ShowTitleBar="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="HeaderTitleTextBlock" Text="Brand breakdown" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock x:Name="HeaderSummaryTextBlock" Margin="0,8,0,0" TextWrapping="Wrap" Opacity="0.85"/>
        </StackPanel>

        <DataGrid x:Name="SourcesGrid"
                  Grid.Row="1"
                  Margin="0,16,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  ColumnWidth="*"
                  RowHeight="34"
                  SelectionMode="Single"
                  SelectionChanged="SourcesGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mod zip / folder" Binding="{Binding DisplayName}" Width="1.6*"/>
                <DataGridTextColumn Header="Configs" Binding="{Binding ConfigCount}" Width="0.45*"/>
                <DataGridTextColumn Header="Flagged" Binding="{Binding ReviewCount}" Width="0.45*"/>
                <DataGridTemplateColumn Header="Assigned configs" Width="2.6*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ConfigNamesText}" TextWrapping="Wrap" Margin="0,4,0,4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock x:Name="FooterTextBlock" VerticalAlignment="Center" Opacity="0.85"/>
            <Button DockPanel.Dock="Right" Content="Close" Width="120" Height="36" Click="Close_Click"/>
        </DockPanel>
    </Grid>
</Controls:MetroWindow>
